server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/contiva
    username: postgres
    password: masterkey
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      "[hibernate.format_sql]": true
      "[hibernate.dialect]": org.hibernate.dialect.PostgreSQLDialect

api:
  security:
    token:
      secret: mqE9a/3KhQWPPser4MOGsfI4u+X6Jj0a6vAfkjBFeJE=
